{
  "name": "shaunhare-co-uk-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth $([ -z \"$CI\" ] && echo --profile cdk-personal)",
    "diff": "cdk diff $([ -z \"$CI\" ] && echo --profile cdk-personal)",
    "deploy": "cdk deploy $([ -z \"$CI\" ] && echo --profile cdk-personal)",
    "destroy": "cdk destroy $([ -z \"$CI\" ] && echo --profile cdk-personal)"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.180.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.7.0"
  }
}
